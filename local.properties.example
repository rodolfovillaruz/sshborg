# Copy this file to local.properties and fill in the values.
# local.properties is gitignored and never committed.

# Path to your Android SDK installation.
# Alternatively, set the ANDROID_HOME environment variable.
sdk.dir=/path/to/android/sdk

# Signing configuration — required only for release builds.
signing.storeFile=/path/to/keystore.jks
signing.storePassword=
signing.keyAlias=
signing.keyPassword=

# Google sign-in for hosts that get their address from a reflector — optional.
# google.clientId is a Google OAuth "Web application" client ID whose authorized redirect URI is
# <google.authBase>/oauth/callback; google.authBase is the reflector Worker's https origin.
# google.clientId=1234567890-abc.apps.googleusercontent.com
# google.authBase=https://reflector.example.com
